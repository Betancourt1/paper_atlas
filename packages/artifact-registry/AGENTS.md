# Artifact registry instructions

M0 contains no registered interactive artifacts. Future additions require a
typed schema, source-backed defaults, a static fallback, keyboard and screen
reader support, Playwright coverage for each state, and an ADR for any new
runtime dependency.

