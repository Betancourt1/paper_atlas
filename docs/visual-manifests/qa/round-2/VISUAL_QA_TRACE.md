# VISUAL_QA round 2 — TRACE

- Manifest reviewed: `VISUAL_MANIFEST_TRACE.md`, revision `3`
- Implementation reviewed: commit `8e27467`
- Explainer reviewed: `packages/test-fixtures/explainers/trace.json`
- Fresh render review: 7 figures at 1440 × 1000 and 390 × 844; no page-level horizontal overflow; all IDs, shared scopes, and placements matched.
- Coverage: 16 / 16 paragraph IDs. Scores are independent 1–10 assessments; this report does not approve publication.

## Paragraph scores

| Paragraph | Engineer | Engineer evidence and actionable finding | Implementer | Implementer evidence and actionable finding |
| --- | ---: | --- | ---: | --- |
| `trace_why_p1` | 7 | `YES` is justified, but Treatment A is not a trajectory topology: it fans generic `compare` edges from one extracted clause, and selected Treatment C is a grouped text panel rather than the promised position-to-position trace. Plan an actual useful/redundant/harmful action sequence with one terminal outcome and two credit overlays. | 7 | `trace_visual_outcome_routes` correctly states the two credit views and shared failed boundary, but renders comparison cards rather than dependent decisions feeding one outcome. Implement the trajectory/overlay topology or rename/select the ledger honestly. |
| `trace_why_p2` | 9 | `NO` is appropriate for the concise process-supervision contrast and research question. The generic directed contingency map adds an unsupported clause sequence; make it non-directional. | 10 | No figure is attached, avoiding an invented taxonomy of supervision methods. No change is needed. |
| `trace_change_p1` | 10 | `YES` correctly separates held-fixed verifier/browser/backbone/corpus from the frozen probe and local-credit additions. Alternatives and source boundaries are complete. | 10 | `trace_visual_credit_assignment_change` visibly groups held-fixed and added components and states that TRACE changes credit assignment, not the browser/reward model/verifier. |
| `trace_change_p2` | 9 | `NO` passes the removal test for the controlled-variable boundary. The contingency map should not sequence independent held-fixed components; use a flat changed/unchanged matrix. | 10 | No figure is attached; the adjacent comparison already supplies the relevant structure without duplication. |
| `trace_mechanism_p1` | 10 | The three-paragraph shared plan correctly specifies prefix splits, frozen gold-answer probe, normalization, adjacent difference, and the probe-not-reward-model boundary. | 7 | `trace_visual_credit_flow` includes the right steps, but the generic operation renderer places every item on one continuous spine. It loses the planned branch after one-step TD credit that separately carries the exact telescoping guarantee and the propagated extension. Render that fork explicitly. |
| `trace_mechanism_p2` | 10 | The plan preserves log-ratio gap closure and positive/zero/negative adjacent credit without inventing magnitude. | 7 | Text and fallback are accurate, but the linear spine does not expose the branch topology that distinguishes exact one-step credit from later propagation. Implement the manifest edges. |
| `trace_mechanism_p3` | 10 | The plan explicitly branches one-step telescoping from short look-ahead propagation before combining with outcome advantage; it states that the guarantee applies only to the one-step component. | 7 | The visual says “Preserve telescoping boundary,” but then numbers it as step 5 before propagation step 6, visually turning a branch-specific guarantee into a stage in one process. Use two lanes/forked edges and record the current topology deviation. |
| `trace_example_p1` | 7 | The shared `YES` scope is sound, but Treatment A sequences the actual trajectory and two alternative credit interpretations with generic `then` edges. Outcome-only comparison and TRACE analysis are not later trajectory events. Plan T0–T3 once, with parallel credit annotations. | 6 | `trace_visual_worked_trajectory` labels search/open/detour/wrong answer T0–T3 correctly, then labels “Outcome-only comparison” T4 and “Example boundary” T5. Those are analytical overlays, not later times. Keep only actions on the timeline and move credit views/boundary outside the temporal axis. |
| `trace_example_p2` | 7 | The same selected sequence does not actually compare outcome-only whole-trajectory credit with local positive/zero/negative credit as parallel readings; it appends the comparison after the events. Redesign as one trajectory with two signal rows. | 6 | Positive/zero-negative descriptions and the failed terminal outcome are present, but the T4/T5 encoding invents temporal order for interpretation and limitation. Replace with aligned outcome-only versus TRACE overlays. |
| `trace_evidence_p1` | 10 | `YES` correctly plans a conditions matrix with both exact backbones, offline corpus, ≤60 turns, four algorithms, browser actions, terminal reward, and evaluation interface. | 10 | `trace_visual_training_conditions` visibly includes every controlled condition and keeps results out of this protocol figure. The mobile table remains contained and accessible. |
| `trace_evidence_p2` | 10 | `YES` separates BrowseComp-Plus base→TRACE from four-benchmark GRPO→TRACE, retains all eight values, both backbones, and disclosed domains. | 10 | `trace_visual_main_results` renders 7.2/35.6, 8.4/42.6, 29.5/34.0, and 32.5/38.1 in separate panels with exact labels and no single ranking claim. |
| `trace_evidence_p3` | 10 | `YES` correctly uses the 29.5–36.0 local domain for 30.0, 32.4, 34.6, and 35.5, and keeps earlier learning outside the numeric axis with single-run uncertainty. | 10 | `trace_visual_credit_ablation` shows all four points on the disclosed domain and a separate qualitative note for earlier learning; the limitation blocks variance-adjusted interpretation. |
| `trace_limitations_p1` | 9 | `NO` is correct for known-answer/short-output/domain-transfer limits. The contingency map’s directed clauses are unsupported; use a flat tested/unestablished boundary. | 10 | No figure is attached, so code/long-response/open-ended transfer is not implied by a generic process. |
| `trace_limitations_p2` | 9 | `NO` passes the removal test for backbone/domain/causality/external-system caveats. Remove directed `then` edges from contingency treatments. | 10 | No visual is attached; readiness, causal necessity, and external-context distinctions remain prose-only. |
| `trace_review_p1` | 9 | `NO` appropriately leaves the narrow controlled conclusion in prose. The stock directed contingency is not content-specific; use a claim/evidence matrix only if needed. | 10 | No figure duplicates the result and mechanism visuals. The narrow conclusion remains textual. |
| `trace_review_p2` | 9 | `NO` correctly preserves the non-general solution, known-answer dependency, single-run uncertainty, and search-only transfer. The contingency map should be non-directional. | 10 | No visual is attached; proposed coding/open-ended transfer remains explicitly unestablished prose. |

## Aggregate producer scores

- `data_visualization_engineer`: **8 / 10**. Quantitative evidence and the central mechanism branch are planned well, but the opening “trace” and worked-example sequence use generic prose-derived topology, and NO contingencies repeatedly add unsupported direction.
- `visual_implementer`: **8 / 10**. Evidence figures, comparison, placement, accessibility, and responsive containment are strong. The core mechanism loses its planned fork, the opening trace becomes cards, and the worked timeline assigns times to analytical overlays.

## Consolidated actionable findings

1. Render the one-step telescoping guarantee and propagated-credit extension as explicit branches after adjacent TD credit.
2. Rebuild the opening and worked example as one action trajectory with aligned outcome-only versus TRACE credit overlays; keep interpretation/boundary items off the time axis.
3. Replace directed `then` maps in prose-only contingencies with non-directional evidence/scope structures.
