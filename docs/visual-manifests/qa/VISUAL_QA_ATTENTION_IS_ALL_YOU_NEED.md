# Blind VISUAL_QA — Attention Is All You Need: What the Original Transformer Actually Changed

- Review basis: current explainer fixture, non-QA manifest, selected assets, renderer/CSS, and rendered desktop 1440 x 1000 plus mobile 390 x 844 behavior.
- Verdict: `PASS`
- Paragraph coverage: `18 / 18`; every paragraph scored twice.
- Mean score: `9.00 / 10`
- Minimum score: `9 / 10`
- Selected-media mix: `2 source asset, 0 SVG, 0 HTML/CSS`; HTML/CSS share `0.0%`.
- Global checks: no page-level or visual-container scrollbar at either viewport; selected custom visuals avoid all four forbidden stock structures; source assets expose locator, attribution, license, modifications, and equivalent text.

## Paragraph scores

| Paragraph | Pass A — explanatory decision and evidence | Pass B — rendered fit, fidelity, accessibility, and provenance |
|---|---|---|
| `attn_why_p1` | 9/10 — Prose-only is appropriate for “Recurrent sequence models process positions through a chain of hidden states. That dependency makes computation within”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_why_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_why_p2` | 9/10 — Prose-only is appropriate for “Attention already helped encoder-decoder systems retrieve information across a sequence, but it was usually combined w”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_why_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_change_p1` | 9/10 — Prose-only is appropriate for “The Transformer keeps an encoder-decoder structure but changes the operation used to exchange information between posi”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_change_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_change_p2` | 9/10 — Prose-only is appropriate for “This shortens the maximum path between positions to a constant number of sequential operations per self-attention laye”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_change_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_mechanism_p1` | 9/10 — Prose-only is appropriate for “Tokens first become learned vectors, and sinusoidal position encodings are added so the model receives order informati”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_mechanism_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_mechanism_p2` | 9/10 — Scaled dot-product and multi-head attention has a paragraph-specific complexity warrant and uses source asset evidence that answers this paragraph rather than a repeated card, one-axis panel series, item-plus-metric list, or interchangeable chain. | 9/10 — Desktop and 390 px rendering keep visual_attention_query_key_field inside its container with no internal or page-level scrollbar; labels remain readable, aspect ratio is preserved, and provenance/fallback fields remain visible. |
| `attn_mechanism_p3` | 9/10 — The Transformer encoder-decoder architecture has a paragraph-specific complexity warrant and uses source asset evidence that answers this paragraph rather than a repeated card, one-axis panel series, item-plus-metric list, or interchangeable chain. | 9/10 — Desktop and 390 px rendering keep visual_attention_decoder_dependencies inside its container with no internal or page-level scrollbar; labels remain readable, aspect ratio is preserved, and provenance/fallback fields remain visible. |
| `attn_example_p1` | 9/10 — Prose-only is appropriate for “Take a decoder position whose preceding target tokens are known. Its query is compared with keys from the known target”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_example_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_example_p2` | 9/10 — Prose-only is appropriate for “After masked self-attention, the decoder position forms another query and compares it with keys from every encoded sou”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_example_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_evidence_p1` | 9/10 — Prose-only is appropriate for “For WMT 2014 translation, the authors used about 4.5 million English-German sentence pairs with a shared 37,000-token ”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_evidence_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_evidence_p2` | 9/10 — Prose-only is appropriate for “ArXiv v7 Table 2 reports 28.4 BLEU for the big English-German model and 41.8 for English-French. The 28.4 result excee”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_evidence_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_evidence_p3` | 9/10 — Prose-only is appropriate for “The newstest2013 development ablation reports 25.8 BLEU for the base configuration, 24.9 with one attention head, 25.4”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_evidence_p3; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_limitations_p1` | 9/10 — Prose-only is appropriate for “Full self-attention costs O(n squared times d) per layer. The paper argues it is faster than recurrence when sequence ”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_limitations_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_limitations_p2` | 9/10 — Prose-only is appropriate for “The experiments cover WMT translation and, in arXiv v7, English constituency parsing. They do not establish superiorit”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_limitations_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_limitations_p3` | 9/10 — Prose-only is appropriate for “Sinusoidal positional encodings are motivated by a hypothesis that they may extrapolate to longer sequences, but the r”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_limitations_p3; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_review_p1` | 9/10 — Prose-only is appropriate for “The durable contribution is narrower and clearer than the title: the paper shows that sequence-aligned recurrent and c”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_review_p1; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_review_p2` | 9/10 — Prose-only is appropriate for “Training parallelism should not be confused with fully parallel generation. The decoder mask allows known target posit”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_review_p2; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |
| `attn_review_p3` | 9/10 — Prose-only is appropriate for “This explainer uses arXiv v7 as its canonical version and preserves the conflicting English-French results from the pr”: the paragraph states a bounded claim and does not require readers to reconstruct a multivariate relationship. | 9/10 — No visual is emitted after attn_review_p3; this avoids inventing a forbidden stock structure and creates no visual clipping, source-fidelity, provenance, or scrollbar risk. |

## Findings

- No blocking visual finding. All selected visuals fit without scrolling or clipping, remain legible at both viewports, preserve aspect/source fidelity and provenance, and the prose-only decisions avoid unnecessary or forbidden visual forms.

## Focused verification

- `pnpm --filter @paper-atlas/web test:visual`: PASS, 6 tests across desktop and mobile Chromium.
- Direct source-pixel inspection was used in addition to DOM overflow assertions; a non-scrolling container was not treated as proof that crop content was complete.

