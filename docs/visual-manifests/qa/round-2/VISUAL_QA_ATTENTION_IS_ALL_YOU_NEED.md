# VISUAL_QA round 2 — Attention Is All You Need

- Manifest reviewed: `VISUAL_MANIFEST_ATTENTION_IS_ALL_YOU_NEED.md`, revision `3`
- Implementation reviewed: commit `8e27467`
- Explainer reviewed: `packages/test-fixtures/explainers/attention-is-all-you-need.json`
- Fresh render review: 11 figures at 1440 × 1000 and 390 × 844; no page-level horizontal overflow; all recorded placements matched the rendered paragraph IDs.
- Coverage: 18 / 18 paragraph IDs. Scores are independent 1–10 assessments; this report does not approve publication.

## Paragraph scores

| Paragraph | Engineer | Engineer evidence and actionable finding | Implementer | Implementer evidence and actionable finding |
| --- | ---: | --- | ---: | --- |
| `attn_why_p1` | 7 | `YES` is appropriate and the boundary against measured latency/autoregressive parallelism is explicit. However, Treatment A omits the self-attention route entirely and connects extracted prose clauses with generic `compare` edges; selected Treatment C is a grouped text panel, not the promised position-to-position trace. Replace the alternatives with actual recurrent-chain, convolution-hop, and direct-attention path marks. | 7 | `visual_attention_path_length` is correctly placed and accessible, but the rendered parallel cards only restate route descriptions; they do not show handoff counts or path topology. Implement the selected trace or choose the ledger honestly instead of calling cards a trace. |
| `attn_why_p2` | 9 | `NO` passes the removal test: the prior-work contrast and research question are concise prose. The contingency set is inspectable, but Treatment C turns the clauses into a generic `then` chain that the evidence does not establish. Remove that invented sequence or make the boundary map non-directional. | 10 | No figure is attached; the prose remains semantic, adjacent to its claims, responsive, and free of visual-only content. No implementation change is needed. |
| `attn_change_p1` | 10 | `YES` correctly isolates encoder self-attention, masked decoder self-attention, and cross-attention. The three treatments are distinct, source-bounded, and specify real inputs, merges, and reading order with complete code. No planning change is needed. | 10 | `visual_attention_routing` appears after this paragraph and preserves the eight-node routing structure, including explicit `Receives` relationships, source/target separation, fallback, evidence, and limitations at both viewports. No change is needed. |
| `attn_change_p2` | 10 | `YES` is justified because readers must separate the shortened attention route from retained feed-forward, residual, normalization, embedding, positional, projection, and softmax components. Alternatives and boundaries are complete. No planning change is needed. | 10 | `visual_attention_retained_components` visibly separates changed position mixing from retained components and states the generation boundary. Placement, readable grouping, fallback, and mobile stacking are correct. No change is needed. |
| `attn_mechanism_p1` | 10 | `YES`, the six-layer repetition, sublayer order, residuals, and normalization are all represented in three distinct architecture treatments with source fidelity and complete specifications. No planning change is needed. | 7 | The fixture contains all required components and `6 layers`, but the generic `ARCHITECTURE_STEPPER` renderer labels each component `Layer 1` through `Layer 6`. This incorrectly makes embeddings, attention, residual normalization, and feed-forward operations look like six different layers before a second `Repeat ×6` statement. Render component steps/sublayers without hard-coded layer numbers and reserve “six layers” for the stack repetition. |
| `attn_mechanism_p2` | 10 | `YES` and the operation-flow treatment faithfully encode QKᵀ, √dₖ scaling, softmax, value mixing, eight heads, concatenation, and projection. Alternatives, accessibility, and evidence limits are complete. No change is needed. | 10 | `visual_attention_scaled_dot_product` renders the six operations in the correct order with `8 heads` visible, selectable labels, evidence, fallback, and responsive behavior. No change is needed. |
| `attn_mechanism_p3` | 10 | `YES` correctly plans a feedback loop that keeps masked attention, cross-attention, feed-forward transformation, projection/softmax, append, and repeat together while preserving the training/generation distinction. No change is needed. | 10 | `visual_attention_generation_loop` is correctly placed, visibly states that each of six decoder layers masks future positions, and renders the return cycle without motion dependence. Fallback and limitations preserve the parallel-training/sequential-generation boundary. No change is needed. |
| `attn_example_p1` | 10 | The shared `YES` scope with `attn_example_p2` is explicit, adjacent, and pedagogically coherent. The worked-sequence treatments preserve masking, scaling, prefix-value mixing, source querying, and prediction. No change is needed. | 10 | The shared `visual_attention_worked_decoder` is placed after `attn_example_p2`, as declared, and covers this paragraph’s prefix comparison, √dₖ scaling, future mask, and prefix-value mixture. Accessible static text is complete. No change is needed. |
| `attn_example_p2` | 10 | The same declared shared visual appropriately completes the decoder walkthrough with cross-attention, source-value mixing, projection, and the training/generation boundary. No planning change is needed. | 10 | The visual includes source querying, source-value mixing, feed-forward/projection/softmax, and the one-token generation boundary; scope, placement, fallback, and mobile order all match the record. No change is needed. |
| `attn_evidence_p1` | 10 | `YES` and the condition-matrix treatments keep English–German and English–French corpora/vocabularies, shared hardware, and the two run lengths visibly grouped rather than treating conditions as results. No change is needed. | 10 | `visual_attention_training_conditions` visibly includes ≈4.5M/37K, 36M/32K, 8 P100 GPUs, 100K/12 h, and 300K/3.5 days in honest groups. It is readable on desktop and horizontally contained on mobile. No change is needed. |
| `attn_evidence_p2` | 10 | `YES` is necessary for the source conflict. All five source-location/value records are explicit, attribution is preserved, and the treatments do not resolve the disagreement. No change is needed. | 10 | `visual_attention_bleu_sources` visibly presents 28.4, 41.8, 41.0, 41.0, and 41.1 against exact primary-source locations, with a limitation that the conflict is not resolved. Placement and fallback are correct. No change is needed. |
| `attn_evidence_p3` | 10 | `YES` correctly separates translation BLEU and parsing F1 into disclosed local domains, prints every reported value, and avoids cross-metric comparison. No planning change is needed. | 10 | `visual_attention_ablation_domains` renders 25.8, 24.9, 25.4, 25.7 and 91.3, 92.7, 93.3 in separate labeled domains with exact values and explicit non-comparability. Mobile removes the marks but retains all values and descriptions, so the static fallback remains complete. No change is needed. |
| `attn_limitations_p1` | 10 | `YES` correctly makes the O(n²d), n < d, constant-path, and O(n/r) restricted-attention trade-off visible without claiming universal speed. The alternatives match the comparison/boundary job. No change is needed. | 10 | `visual_attention_complexity_boundary` visibly separates full attention, the conditional speed argument, and the restricted-attention future-work trade-off. It preserves formulas, limitations, and source links at both widths. No change is needed. |
| `attn_limitations_p2` | 9 | `NO` is correct because the tested-versus-unestablished scope is already short, explicit prose. The contingency treatments remain generic stock panels/ledger/map forms and the map can imply a clause sequence. Replace the directed contingency with a non-causal scope boundary if retained. | 10 | No visual is attached, so the limitations remain prose-only and do not acquire invented scale or generalization. Desktop/mobile reading order is correct. No implementation change is needed. |
| `attn_limitations_p3` | 9 | `NO` is appropriate for the two compact epistemic boundaries. As above, the contingency map’s generic directed clause sequence is not evidence-backed. Make it a non-directional tested/not-established matrix or remove the direction. | 10 | No figure is attached; sinusoidal extrapolation and attention-faithfulness caveats remain explicit text with no unnecessary visual. No change is needed. |
| `attn_review_p1` | 9 | `NO` correctly leaves the narrow historical conclusion in prose, but the reusable contingency set is not fully content-specific and its directed map can imply unsupported progression. Replace it with a claim/support boundary if a contingency is needed. | 10 | No visual was added; the durable-contribution boundary remains prose-only, source-linked, and responsive. No change is needed. |
| `attn_review_p2` | 9 | `NO` passes the removal test, but the generic panel/ledger/directed-map contingencies do not all meet the manifest’s content-specific topology standard. Remove the `then` topology from the contingency map. | 10 | The training-versus-generation warning remains prose-only and no visual duplicates the earlier generation loop at this paragraph. No change is needed. |
| `attn_review_p3` | 9 | `NO` is correct for provenance/license prose; the three contingencies are unnecessary and the directed map again invents order among source-version and permission statements. Use a non-directional provenance matrix if this ever becomes `YES`. | 10 | No figure is attached; version conflict and reuse provenance remain selectable text with source links. No implementation change is needed. |

## Aggregate producer scores

- `data_visualization_engineer`: **9 / 10**. Coverage, evidence references, shared scopes, numeric-domain planning, and reproducible code are strong. The main deductions are the non-topological “position-to-position trace” for `attn_why_p1` and generic directed contingency maps in otherwise-correct NO records.
- `visual_implementer`: **9 / 10**. All IDs, placements, accessibility surfaces, fallbacks, limitations, and responsive containment are present. The two material weaknesses are the prose-card substitute for the path trace and the misleading hard-coded `Layer 1…6` labels inside one encoder layer.

## Consolidated actionable findings

1. Rebuild `visual_attention_path_length` as an actual three-lane path comparison with visible recurrent handoffs, convolutional hop growth, and a direct self-attention route.
2. Change the architecture renderer so component rows are steps/sublayers, not automatically numbered as layers; keep the `×6` repetition as the only six-layer statement.
3. Remove unsupported `then` edges from prose-only contingency maps and use non-directional claim/scope structures when a contingency is retained.
