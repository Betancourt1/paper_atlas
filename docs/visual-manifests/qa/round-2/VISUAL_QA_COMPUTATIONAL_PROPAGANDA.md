# VISUAL_QA round 2 — Computational Propaganda

- Manifest reviewed: `VISUAL_MANIFEST_COMPUTATIONAL_PROPAGANDA.md`, revision `3`
- Implementation reviewed: commit `8e27467`
- Explainer reviewed: `packages/test-fixtures/explainers/computational-propaganda.json`
- Fresh render review: 6 figures at 1440 × 1000 and 390 × 844; no page-level horizontal overflow; all recorded placements matched the rendered paragraph IDs.
- Coverage: 16 / 16 paragraph IDs. Scores are independent 1–10 assessments; this report does not approve publication.

## Paragraph scores

| Paragraph | Engineer | Engineer evidence and actionable finding | Implementer | Implementer evidence and actionable finding |
| --- | ---: | --- | ---: | --- |
| `propaganda_why_p1` | 9 | `NO` is correct: the crawl/extraction/filter qualification is already a concise verbal boundary. The contingency map nevertheless converts clauses into a generic directed sequence. Make any future contingency a non-directional threat-model boundary rather than a `then` chain. | 10 | No figure is attached, so the paragraph remains prose-only and no unsupported crawler topology or scale is introduced. It reads correctly at both viewports. No change is needed. |
| `propaganda_why_p2` | 9 | `NO` appropriately preserves the attacker-access boundary as prose. The three contingencies are inspectable but formulaic, and the directed map can imply an evidentiary progression among independent exclusions. Remove that direction if retained. | 10 | No figure is attached; the attacker’s unknowns and unavailable assets remain explicit text with no redundant visual. No implementation change is needed. |
| `propaganda_change_p1` | 10 | `YES` passes the removal test because three conditional inclusion questions must be distinguished from mere posting access. The alternatives specify the actual gates, evidence boundary, accessibility, and complete code. No change is needed. | 10 | `propaganda_visual_inclusion_questions` is adjacent and renders posting access, S1, S2, S3, and the conditional estimate in order, while its limitation restricts the claim to the tested collection path. Mobile and fallback are complete. No change is needed. |
| `propaganda_change_p2` | 10 | `YES` correctly plans a parallel comparison between comments and programmatic advertisements with an explicit architecture boundary; no universal crawler claim is implied. No planning change is needed. | 10 | `propaganda_visual_comments_vs_ads` visibly shows 71.9% comment survival, the negative advertisement observation, and the tested-architecture boundary in separate panels. Placement, grouping, and accessibility are correct. No change is needed. |
| `propaganda_mechanism_p1` | 10 | The shared `YES` plan correctly encodes S1/S2/S3, denominators, the combined estimate, and a genuinely separate controlled model-influence branch. Treatment A’s code contains two distinct routes converging only on the “not established” boundary. No change is needed. | 7 | The shared `propaganda_visual_halflife_flow` contains the right labels and values, but the generic operation renderer flattens all eight items onto one continuous numbered spine. That topology makes controlled model training look downstream of the inclusion estimate despite the title and prose saying it is separate. Render two disconnected lanes or a true branch with an explicit no-join boundary. |
| `propaganda_mechanism_p2` | 10 | The declared three-paragraph shared scope is valid, and the plan keeps the page, simulated-injection, and captured-document denominators distinct before combination. No planning change is needed. | 7 | The same flattened shared visual preserves denominators in text but not the planned two-branch topology. The continuous 1–8 sequence visually joins the inclusion and influence experiments. Implement the manifest’s branch structure rather than relying on group labels to undo the spine. |
| `propaganda_mechanism_p3` | 10 | The plan explicitly treats corpus inclusion as intermediate and model influence as a separate controlled experiment, with no live web-to-model claim. Alternatives and evidence limits are complete. No change is needed. | 7 | The separate-experiment and no-live-attack statements are visible, but the continuous numbered connector still implies one end-to-end path. Split the visual topology and preserve the boundary as a limitation, not as the final step of a single process. |
| `propaganda_example_p1` | 10 | The shared `YES` worked example is correctly placed after the second paragraph and includes 37.5 words, 71.9% extraction survival, the later filters, and denominator conditions. No planning change is needed. | 10 | `propaganda_visual_worked_inclusion` includes the sandbox replacement, 37.5-word fragment, extraction, and the start of the controlled gate calculation in source order. Shared placement and fallback are correct. No change is needed. |
| `propaganda_example_p2` | 10 | The plan completes the worked calculation with 5.5%, 3.4%, 71.9%, ≈0.13%, the conflicting 0.15%, and the non-live-campaign boundary. No planning change is needed. | 10 | The visual shows every stated percentage, distinguishes the derived and disputed values by role, and ends with an explicit campaign boundary. It remains readable and contained on mobile. No change is needed. |
| `propaganda_evidence_p1` | 9 | `YES` and the disclosed-domain design correctly distinguish measured gates, derived product, and disputed value. Treatment A’s Python data retains `181,857`, `200 WARC files`, and `CC-MAIN-2025-51`, but its TikZ/Mermaid omit the latter two protocol conditions; all three code forms should expose the same evidence. Add those conditions visibly across treatments. | 8 | `propaganda_visual_inclusion_evidence` correctly shows 3.4%, 71.9%, 5.5%, ≈0.13%, and 0.15% on separate honest domains, but it drops the manifest-planned sample record (`181,857 pages`, `200 WARC files`, `CC-MAIN-2025-51`) entirely. Add a visible protocol/sample note or matrix row and record the current deviation until fixed. |
| `propaganda_evidence_p2` | 8 | The plan correctly treats 18.6–20.7 as a range and declares the shared scope with `p3`, but its selected treatment omits two of the three controlled poison rates (`0.001%` and `0.01%`) even though the scope requires every listed condition/value. Add a visible rate-set condition while keeping the 0.1%-specific effect range distinct. | 8 | `propaganda_visual_model_effects` shows model scale, the 0.1% condition, and 18.6–20.7 pp, but neither `0.001%` nor `0.01%` appears in the visual, alt text, or fallback. Add the full controlled rate set without implying effect values for the lower rates. |
| `propaganda_evidence_p3` | 10 | The shared treatment faithfully specifies the 0.9–7.4 post-SFT range and separate 0.1/0.3 no-label points with disclosed local domains. No planning change is needed. | 10 | The shared result plot visibly separates base-model, post-SFT, and no-label post-SFT groups and prints 0.9–7.4, 0.1, 0.3, 709M, and 1.3B with honest domains. No change is needed. |
| `propaganda_limitations_p1` | 9 | `NO` is correct because the proxy/open-pipeline/proprietary-crawler caveats are explicit prose. The generic directed contingency map can imply sequence among independent limitations. Use a non-directional scope matrix if visualized. | 10 | No visual is attached, preventing invented comparisons among proprietary systems and the tested open pipeline. Prose remains responsive and source-linked. No change is needed. |
| `propaganda_limitations_p2` | 9 | `NO` appropriately leaves the sandbox, model-scale, universal-rate, and frontier-persistence boundaries in prose. The stock contingency map again adds unsupported direction. Remove the directed edges in any future treatment. | 10 | No figure is attached; the non-established live-attack and frontier-scale claims remain textual and cannot be mistaken for measured outcomes. No change is needed. |
| `propaganda_review_p1` | 9 | `NO` is a sound editorial decision for the bounded methodological conclusion. The contingency set is reusable rather than fully tailored, and its directed map is not warranted. Replace it with an evidence/support boundary only if a visual later becomes necessary. | 10 | No figure duplicates the earlier method visuals; the conclusion and negative-advertisement interpretation remain prose-only. No implementation change is needed. |
| `propaganda_review_p2` | 9 | `NO` correctly preserves the title/threat-model qualification as prose. The contingency map’s clause sequence is not evidence-backed; make it non-directional if retained. | 10 | No visual is attached; 0.15% versus 0.13%, the separate model experiment, and the unestablished production attack remain explicit prose. No change is needed. |

## Aggregate producer scores

- `data_visualization_engineer`: **9 / 10**. The central method, shared scopes, conditional denominators, conflicting values, local domains, and branch boundary are well planned with complete reproducible code. Deductions come from incomplete protocol parity across treatment code for `evidence_p1`, omission of two poison-rate conditions in `evidence_p2`, and generic directed contingency maps for NO records.
- `visual_implementer`: **8 / 10**. IDs, placements, accessibility, fallbacks, limitations, responsive containment, and most numeric values are strong. The core method visual loses the planned separate-branch topology, and two evidence visuals omit declared protocol/rate conditions.

## Consolidated actionable findings

1. Render the inclusion-estimate path and controlled model-influence experiment as two visibly separate lanes/branches; remove the single continuous numbered spine across both.
2. Restore the visible `181,857 pages / 200 WARC files / CC-MAIN-2025-51` protocol record in the inclusion evidence visual.
3. Add `0.001%`, `0.01%`, and `0.1%` as the full tested poison-rate set while clearly tying the plotted effect ranges only to `0.1%`.
4. Replace directed `then` maps in prose-only contingencies with non-directional boundary structures.
