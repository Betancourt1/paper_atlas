# VISUAL_QA round 2 — Inkling

- Manifest reviewed: `VISUAL_MANIFEST_INKLING.md`, revision `3`
- Implementation reviewed: commit `8e27467`
- Explainer reviewed: `packages/test-fixtures/explainers/inkling.json`
- Fresh render review: 9 figures at 1440 × 1000 and 390 × 844. Placements matched. Desktop had no page overflow; the mobile document expanded to 450 px in a 390 px viewport because long source locators/hashes in every figure evidence section did not wrap.
- Coverage: 19 / 19 paragraph IDs. Scores are independent 1–10 assessments; this report does not approve publication.

## Paragraph scores

| Paragraph | Engineer | Engineer evidence and actionable finding | Implementer | Implementer evidence and actionable finding |
| --- | ---: | --- | ---: | --- |
| `ink_why_p1` | 9 | `NO` is appropriate for the provider-positioning statement. The contingency map is a generic directed clause chain rather than a content-specific non-causal boundary; make it non-directional if retained. | 10 | No figure is attached, so positioning and intended uses remain prose-only without invented benchmark scale. No change is needed. |
| `ink_why_p2` | 9 | `NO` passes the removal test, but its stock panel/ledger/directed-map contingencies do not all meet the content-specific topology standard. Remove the `then` topology. | 10 | No visual is attached; “not strongest overall” and the proposed value bundle remain explicit prose. No implementation change is needed. |
| `ink_change_p1` | 10 | `YES` correctly separates 975B stored capacity, 41B active computation, routed/shared expert counts, and the deployment-storage boundary. Treatments and code preserve grouping and scope. | 7 | `visual_inkling_total_vs_active` visibly presents 975B, 6/256, 2 shared, and 41B with honest grouping, but its long content-addressed evidence links overflow the figure and expand the 390 px page to 450 px. Allow evidence locators to wrap anywhere without losing selectable text. |
| `ink_change_p2` | 9 | `NO` is correct for the bounded effort-control claim. The directed contingency map can imply a process not established by the prose. Replace it with a changed/unchanged boundary if ever used. | 10 | No figure is attached, avoiding an unsupported effort-performance curve. No implementation change is needed. |
| `ink_mechanism_p1` | 10 | `YES` and the convergence plan correctly route one token to 6 of 256 experts plus 2 always-active experts before a merge; all three code forms preserve the branch topology. | 7 | `visual_inkling_moe_routing` preserves the router/shared branches, convergence, and 41B active path through explicit `Receives` text, but its evidence hashes overflow on mobile. Add robust wrapping to source links/locators. |
| `ink_mechanism_p2` | 10 | `YES` faithfully plans text, image, and audio front ends converging into shared space, then the 5:1 local/global stack, 8 KV heads, 40×40 patches, four layers, and 66-layer decoder. | 7 | `visual_inkling_multimodal_convergence` includes all modality and architecture values and keeps source descriptions distinct, but the evidence section creates page-level mobile overflow. Fix long-token wrapping. |
| `ink_mechanism_p3` | 10 | `YES` correctly separates the provider-reported 45T-token pretraining → synthetic SFT → reinforcement-learning sequence from the disclosure boundary. | 7 | `visual_inkling_training_sequence` preserves stage order, checkpoint release, and the missing-inventory boundary; however, its unbroken source locators overflow the mobile viewport. Make citations responsive. |
| `ink_example_p1` | 10 | `YES` and the checkpoint matrix retain BF16/NVFP4 modes, ≥2 TB/≥600 GB, GPU counts, SM100+, retrieval mutability, and absent precision-quality parity. | 7 | `visual_inkling_checkpoint_hardware` visibly includes all hardware conditions and the transfer boundary, but the mobile page still widens because citation hashes cannot wrap. Repair evidence-link wrapping. |
| `ink_example_p2` | 9 | `NO` correctly leaves the quantization-transfer warning in prose, but the contingency map again adds an unwarranted directed clause sequence. Use a non-directional parity boundary if visualized. | 10 | No second figure is added; the checkpoint matrix immediately above already supplies context without duplicating this caveat. No change is needed. |
| `ink_evidence_p1` | 10 | `YES` correctly groups all seven selected scores by incomparable family, keeps both HLE conditions, and explicitly rejects a common leaderboard axis. | 7 | `visual_inkling_benchmark_families` uses separated groups with 29.7%, 46.0%, 77.6%, 79.8%, 73.5%, 91.4%, and 98.6%; the visual itself is honest, but its evidence hashes overflow the 390 px render. Fix wrapping. |
| `ink_evidence_p2` | 9 | `NO` is reasonable because the protocol qualifications are heterogeneous and already explicit. The generic contingency sequence should be non-directional. | 10 | No visual is attached, avoiding a misleading single protocol flow across unrelated harness caveats. No implementation change is needed. |
| `ink_evidence_p3` | 10 | `YES` correctly maps the effort sweep and forecasting result to earlier/different checkpoints and prevents silent transfer to BF16/NVFP4 releases. | 7 | `visual_inkling_checkpoint_results` visibly preserves the checkpoint mismatch and released-checkpoint boundary, but the evidence locator text overflows mobile. Add `overflow-wrap: anywhere` or an equivalent accessible strategy. |
| `ink_limitations_p1` | 9 | `NO` is correct for a heterogeneous list of provider caveats and safeguards. The directed contingency map is not an evidence-backed sequence; replace it with a flat scope ledger if needed. | 10 | No visual is attached; safety and quality limitations remain prose-only. No change is needed. |
| `ink_limitations_p2` | 10 | `YES` correctly distinguishes advertised 1M capacity, 64K/256K hosted options, unproven accuracy span, BF16/NVFP4 parity, and fine-tuned safety transfer. | 7 | `visual_inkling_context_boundary` presents the capacity/access/unestablished groups clearly, but all long source locators force mobile horizontal scrolling. Fix responsive citation wrapping. |
| `ink_limitations_p3` | 9 | `NO` passes the removal test for missing-disclosure prose. The contingency map’s clause direction is unsupported. Use a non-directional missing-evidence matrix if changed to `YES`. | 10 | No figure is attached, so absent dataset details are not converted into invented quantitative marks. No change is needed. |
| `ink_limitations_p4` | 9 | `NO` is acceptable because the paragraph already explains mutability and fixed retrievals. The generic directed contingency map should be replaced with a provenance matrix if ever implemented. | 10 | No local visual is attached; the detailed provenance figure remains reserved for the later review paragraph. No change is needed. |
| `ink_review_p1` | 9 | `NO` is appropriate for the supported-versus-rejected conclusion, but the stock directed-map contingency can imply progression among independent boundaries. Make it non-directional. | 10 | No visual duplicates the preceding mechanism, benchmark, and limitation figures. No implementation change is needed. |
| `ink_review_p2` | 8 | `YES` is justified, but the selected source-by-value matrix calls every source identity merely `qualitative`; it says hashes/commits exist without printing the actual SHA-256, byte counts, or pinned commit IDs despite promising exact locators. Add the fixed identities or a compact, copyable prefix plus full accessible value. | 6 | `visual_inkling_source_provenance` shows only “Content-addressed HTML” and “Pinned commit,” so the explanatory body/fallback cannot identify the snapshots. Exact identities appear only in extremely long evidence locators, which also cause mobile overflow. Put exact fixed identifiers in the matrix/fallback and make them wrap/copy cleanly. |
| `ink_review_p3` | 9 | `NO` is correct for the Apache 2.0/AUP qualification. The generic directed contingency map can imply precedence between legal statements; use a side-by-side terms boundary instead. | 10 | No figure is attached, preventing the licensing qualification from being simplified into an unsupported hierarchy. No change is needed. |

## Aggregate producer scores

- `data_visualization_engineer`: **9 / 10**. Decisions, architecture topology, quantitative grouping, checkpoint boundaries, and code are strong. Deductions come from generic directed contingencies and a provenance treatment that does not actually expose the promised fixed identifiers.
- `visual_implementer`: **7 / 10**. Scientific grouping and placements are mostly faithful, but every implemented figure breaks the 390 px responsive boundary through unwrapped hashes/locators, and the provenance figure withholds exact identities from its visual body and fallback.

## Consolidated actionable findings

1. Make all figure evidence links and long content-addressed locators wrap within the figure; verify that `documentElement.scrollWidth` stays at 390 px.
2. Print exact snapshot/commit identities in `visual_inkling_source_provenance` and its fallback rather than relying on overflowing citations.
3. Replace directed `then` maps in prose-only contingencies with non-directional scope/evidence structures.
